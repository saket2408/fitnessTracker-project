spring:
  application:
    name: feign-client-service      
      
server:
  port: 8010
  
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management: 
  endpoints: 
    web: 
      exposure: 
        include: 'hystrix.stream'
      